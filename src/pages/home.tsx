import React from 'react';
import { Helmet } from 'react-helmet';

const Home = () => {

    return (
        <main>
            <Helmet>
                <title>Home page | react CMS</title>
            </Helmet>

            <h1>Welcome!</h1>
        </main>
    );
};

export default Home;